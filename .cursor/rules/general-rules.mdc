---
alwaysApply: true
---
# Code Standards

- Never launch and never build the app, user doing this manually.
- Use `bun check` to check the codebase for issues. This will run the type checker, the linter, and other checks.
