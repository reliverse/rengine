import { memo } from "react";
import { PlaceholderAssetTab } from "../shared/placeholder-tab";

export const ScriptsTab = memo(function ScriptsTab() {
  return <PlaceholderAssetTab assetType="scripts" />;
});
