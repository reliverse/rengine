import { memo } from "react";
import { PlaceholderAssetTab } from "../shared/placeholder-tab";

export const FontsTab = memo(function FontsTab() {
  return <PlaceholderAssetTab assetType="fonts" />;
});
